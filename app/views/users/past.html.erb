<%= form_for(@user) do |f| %>
  <%= f.label :対象月 %>
  <%= f.date_select :created_at, {:discard_day => true,
                :use_month_numbers => true, start_year:2018, end_year:(Time.now.year)}, class: 'form-control' %>
<% end %>